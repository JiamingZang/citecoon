Agent 构建思想 & 方法
一、Agent 定义
Agent 是由 LLM 驱动、动态指导自身工作流程与工具调用、自主控制任务完成方式的系统；Workflow 则是通过预定义代码路径编排 LLM 和工具的系统。
● 最大区别：Workflow 路径可预测，适合确定性场景；Agent 更适合需要泛化性与自主决策的场景

二、Agent 组成
Agent 的基本组成，是给 LLM 增强检索、工具、记忆等能力，让 LLM 主动调用这些能力完成任务。
注：除 LLM 这个"大脑"外，其余组件与能力都不是必选项

三、Agent 生态
对于生产级应用，Agent 本身的推理与决策能力只是起点。要让它在真实场景中稳定、可靠、高效地运行，还需要一整套配套基础设施——从任务编排到评测观测，从上下文与记忆管理到中断恢复，从知识供给到安全与成本控制，共同构成 Agent 从"能跑"到"可用于生产"的底座。

● 编排（Orchestration）——解决"多步骤、多工具、多 Agent 如何协同"，管理调用顺序与数据流转，避免流程失控或死循环

    ○ 场景复杂度上升后单 Agent 往往无法解决，需要多 Agent，而多 Agent 首先要解决的就是彼此如何协同
    ○ 常见编排模式：Sequential、Parallel、Loop、Orchestrator-Workers、Evaluator-Optimizer、Fan-In/Fan-Out、Agentic（纯 LLM 自主决策）
● 评测（Evaluation）——解决"改动是变好还是变差"，为非确定性行为提供量化基准，上线前发现回归

    ○ 评测是 Agent 演进好与坏的胜负手：Agent 不是一锤子买卖，需要持续迭代优化，每次迭代的效果好坏要靠评测给答案
    ○ 可套用工程领域 TDD（测试驱动开发）的"测试先行"理念：确定好 Agent 要解决的问题场景后，紧接着就应设计如何评测，由评测驱动 Agent 迭代
● 观测（Observability）——解决"出问题时黑盒难查"，记录推理轨迹、工具调用与 token 消耗，让异常可定位、可复现、可归因，从而可修复
● 上下文管理（Context Management）——解决"窗口有限但信息无限"，决定放什么、如何压缩与检索，兼顾关键信息与成本噪声
● 记忆（Memory）——解决"一关就忘、跨会话没积累"，把偏好、结论、踩过的坑持久化复用，让 Agent 越用越懂
● 自进化（Self Evolving）——近期热门、也是重点探索方向之一：让 Agent 通过感知环境、从执行轨迹中抽取优质知识反哺系统，形成运行→沉淀→增强的自演进循环
● 知识库与检索（RAG）——解决"需要私有领域知识但不能全塞进上下文"，外挂可更新、可溯源的知识层，按需拉取准确事实
● 中断恢复（Durable Execution）——解决"长程任务中途崩溃就前功尽弃"，让执行状态可持久化，崩溃重启后从断点续跑
● 人机交互（Human-in-the-Loop）——解决"不能完全自主、关键决策需人把关"，在高风险环节引入人工确认，兼顾效率与可控
● 运行时（Runtime）——解决"逻辑需要稳定承载与调度环境"，负责生命周期、并发调度与资源分配，让 Agent 成为可长期驻留、可对外服务的进程
● 沙箱（Sandbox）——解决"执行不可信代码或操作的安全风险"，为工具调用与代码执行提供隔离环境，防止波及宿主与用户数据
● 安全护栏（Guardrails）——解决"输入被注入、输出越界"，在输入输出两端做校验、脱敏与 prompt injection 防护，是沙箱之外的逻辑层防线
● 成本与模型路由（Model Gateway）——解决"token 花销失控、单一模型不够用"，按任务难度路由模型、复用缓存、设配额上限，平衡效果与开销
四、Agent 构建方法
4.1 选择合适的场景
先厘清几条构建原则：
● 定义问题，而非定义方案：对大多数同学来说，工作重心是解决具体业务问题，Agent 是解决方案而不是问题本身，应从"要解决什么问题"出发，而不是上来就说"我要做某某 Agent"。做好 Agent 的重要前提是有准确清晰的目标，这样优化才有方向（鼓励多用 Agent 解决实际业务问题，但前提是先理清要解决什么）
● 保持简单：应对具体问题的方案应尽可能简单——可预测问题不用 Agent，单步推理能解决就不用多步，单 Agent 能解决就不用多 Agent
● Agent 与 Workflow 的取舍：追求确定性、可控性与成本控制走 Workflow；需要泛化性、动态性、灵活自主处理任务走 Agent
适合 Agent 的场景（可探索的空间是穷举不完的，未来随行业发展会越来越多，鼓励积极尝试、不要束缚思路）：
● 理解模糊意图：把模糊的用户意图转换为可执行的任务
● 开放和动态性：动态执行、执行路径难以穷举的工作流，或开放性问题
● 处理非结构化数据：将非结构化数据转换为结构化输出，比如处理一段非结构化的生产日志，提取关键内容转成结构化输出
● 智能化重构：过去很多传统软件功能其实不是在"交付结果"，而是把大量决策甩给用户——比如商家在淘天发品、报营销活动都要填一大堆表单，本质是"甩锅"式设计，把很多决策让商家来做，操作成本很高。中小商家往往没能力请专业运营，只能硬着头皮上，很吃自身经验。可以用 Agent 重构这类场景：Agent 理解商家意图、直接交付一个结果，商家确认后即可执行，从而把平台意志与优质经营经验以知识形式反哺给商家，帮他们更简单高效地经营店铺
● 自进化：在运行中感知环境、沉淀经验，具备"越干越好"的能力
一个具体场景——生意管家数据分析 Agent：
● 目标用户：在淘天经营的商家
● 背景痛点：商家每天上班第一件事是查看店铺经营情况，需结合千牛、生意管家、万相台等多个系统和大量数据指标做汇总分析，每日耗时以小时计（且随体量、行业、人员组成而异）；大多数非头部商家没有专业 BI 同学、缺乏数据分析知识，经营决策基本"靠感觉"，难以做出有成效的决策
● 目标：帮助商家解决日常经营分析决策过程中的效率和效果问题
● Agent 架构：
    ○ 意图识别模块：生意管家里有很多角色和功能，商家输入指令后，先要识别这个问题适合用"什么角色什么功能"来解决，识别完再路由到具体 Agent 执行
    ○ 主 Agent：承担 Planning 角色，结合电商经营知识库产出解决问题的具体执行计划，并决定调用哪一类子 Agent。数据分析 Agent 支持多行业多场景，特定场景知识量大且面向特定场景，不适合直接进入 Agent 上下文，更适合外挂到知识库、通过检索召回注入上下文
    ○ 子 Agent（面向特定场景设计）：数据分析 Agent 按场景拆分商品、流量、竞店、市场等能力项，解决具体分析场景的问题；数据查询 Agent 专注多周期、多维度、多指标的高效灵活查询，含历史与实时查询
    ○ 店铺巡检：用 Workflow 把商家关注的核心指标、经营数据、竞争分析编排为固定流程，形成完整巡检信息呈现给商家，其中部分节点会具体执行一个 Agent——这体现了 Workflow 与 Agent 在实际场景中常相互配合，固定可预测路径用 Workflow 搞定，确定性更强
● 上线反馈：部分商家反馈每日数据分析耗时从小时级降到十几分钟（主要是中小商家在用；头部 KA 商家一般有专业 BI 团队，不太信任 AI 的分析结论，这也是后续要持续攻克的问题）
● 一个额外话题——隐性知识：数据分析这类专业领域，很多领域知识存在 BI 同学脑子里，是隐性知识；AI 学不到这些知识，也是很多场景 AI 难达人类水准的重要因素，如何让 AI 学习这类隐性知识是 Agent 开发的难点
● 下一步方向（迭代闭环）：当前设计并未真正交付结果，只给出带一定决策建议的分析结论，并没真正解决问题。重要的迭代方向是帮商家做决策并产生具体动作——例如分析发现某商品销量骤降是因竞争对手搞促销吸走了用户，就可决策跟进类似活动，商家同意后把活动配置出来投入生产，再关注活动效果是否解决问题，收集反馈数据优化 Agent，进入下一轮。即形成：分析 → 决策 → 实施 → 反馈 → 优化 的循环
4.2 构建 AI-Friendly 工具
工具是 Agent 感知外界环境的方式，也是 Agent 最常用的组件，工具质量会很大程度上影响 Agent 的运行效果。
工具构建原则：
1. 定义清晰无歧义：名称、描述、入参、出参都要满足。（反例：人都看不懂的缩写，如返回一个叫 afsAmt 的字段）
2. 功能不冲突：给 Agent 挂多个工具时，工具间的职责与功能尽量不要冲突，否则会给 Agent 选择工具增加难度
3. 只暴露该被感知的内容：只让 Agent 看到它需要看到的出入参，如工具返回 10 个字段但 Agent 只需 5 个，就不要把其余 5 个暴露出来
4. 协议结构尽量简单：出入参的结构与层级尽量扁平，入参含复杂嵌套结构会降低调用准确率
5. 使用姿势对 Agent 友好：与其给商品列表工具让 Agent 分页翻找目标商品，不如直接给一个主动搜索商品的工具
6. 工具数量过多时，考虑使用 find tool：多少算多没有绝对数值，要看评测结果——随着迭代可能发现工具变多后 Agent 的工具调用准确率逐渐下降
● 优先考虑拆分 Agent 来解决；拆不了或不适合拆时，再用 find tool
● find tool 思路：按工具的使用频率与泛用性，把最常用的工具默认加载进上下文，其余默认不加载；提供一个 find tool，让模型根据具体问题和上下文动态查找适合的工具，再调用一个"执行被动态查找工具"的工具来完成具体调用
集团发展多年沉淀了大量业务 API，如电商场景的订单、商品、物流等核心要素都有现成 API，但其中很多并不适合直接给 Agent 使用——因为它们是面向传统软件系统或人设计的，对 Agent 并不友好。
AI Studio 工具组件：集团内有很多工具平台/MCP 平台（如 zetta、tmcp、aone mcp 等），AI Studio 工具组件区别于它们的核心特性是不做通用工具平台，而是提供满足上述原则的 AI-Friendly 工具。具体手段包括：
● 通过字段别名解决命名对 AI 不友好的问题
● 通过裁剪、参数固定值等方式，让 Agent 只感知到它需要感知的内容
● 通过不被 Agent 感知的变量来保证工具入参的安全性
● ...
由此最大范围地以简单配置复用已有 API

4.3 记忆与知识沉淀
"自进化"以及"如何让 Agent 越用越好"，很大程度上要靠记忆来解决。
业界通常把记忆分为长期与短期：短期记忆是 Agent 感知到的单个会话上下文，长期记忆是跨会话的知识沉淀；日常所说的"记忆"一般指长期记忆。
记忆适合解决的问题（同样穷举不完，鼓励积极探索）：
1. 用户的画像、习惯、偏好跨会话被遗忘：例如用户在一个会话里跟 Agent 说明处理偏好——回复要简洁、写代码用 Python，本会话按要求处理完了；下个会话再来处理任务，结果 Agent 又啰里吧嗦一大堆、也没按偏好走，这就是跨会话遗忘
2. 用户反复纠正的错误被重复再犯：使用 Agent 完成任务往往需要反复纠正、澄清，这些过程在单会话长时间使用或跨会话场景中会被遗忘，导致用户反复重来
3. 好的执行经验没有被保留，后续相同场景无法保证走最佳路径：同一问题场景 Agent 多次执行的效果不一定稳定，即使结果一致过程也可能不同——就像一道算法题解法有很多种，但成本效率最佳的往往只有一种，如何让 Agent 尽可能以最优路径解决问题也是记忆能解决的
4. 工具、Skill 等组件的调用经验无法复用：Agent 对工具/组件的调用可能因设计问题需反复尝试才能找到合适姿势，这些反复尝试是额外消耗，如何让后续任务能直接干、不再反复试错，也是记忆要解决的问题
AI Studio 记忆系统能力视图：

做记忆方案时可以从下面几个要素来考虑：
● 记忆的分层（业界有各种定义，图上是一个示例，并非绝对通用）

    ○ 第一层 核心记忆：承载用户的画像与习惯偏好，类比人的工作记忆/肌肉记忆——不用刻意回忆、做事时自动加载，一般承载通用、泛用性强的内容
    ○ 第二层 情景记忆：承载特定场景解决问题的经验，按具体问题场景动态加载，类比人置身特定情景解决特定问题时的回想
    ○ 第三层 经验记忆：承载面向特定工具或 Skill 等组件的使用经验
    ○ 补充做法：业界也有实现是按一定维度定期对会话做总结（如每天一次）作为记忆内容的补充
● 记忆的维度（作用域）

    ○ 通常理解记忆属于"人"（用户）
    ○ 随着自演进概念兴起，维度也在延伸：AI Studio 在"人"之上有"空间"概念，可把通用记忆放在空间维度，让空间下所有人都能复用同一份记忆
● 记忆的结构（对记忆效果影响非常大）

    ○ 需能表达它作用于哪个具体的工具/Skill
    ○ 记忆内容如何表达才能有更好的遵循效果
    ○ 是否需要版本管理以具备可回溯能力
    ○ 面向检索召回该如何设计以保证召回效果
● 记忆的生命周期管理（提取策略在内的完整生命周期）

    ○ 记忆的提取、整理
    ○ 记忆冲突如何处理
    ○ 记忆的淘汰机制
● 记忆的加载策略
    ○ 存储用文件系统 → 面向文件系统设计，用文件检索方式加载
    ○ 存储用支持向量检索的数据库 → 用 BM25、ANN 向量检索等方式设计，常见做法是用多路检索提升召回效果
● 记忆的存储：目前常见的是文件系统或向量存储两种，具体按策略选择
● 记忆的评测：涉及评测都是重要内容——如何验证记忆的效果好不好（以 ideaWork 记忆为例）

    ○ 召回率：该召回的记忆有没有被召回、召回的有效记忆占比多少

        ■ extraction recall：该沉淀的记忆有没有进库，表达记忆抽取的效果
        ■ retrieval recall@k：库里有的记忆有没有被捞进 top-k，表达记忆加载的效果
    ○ 任务成功率：记忆召回后，该解决的问题有没有被解决、解决问题的比例多少
五、Reference
● Agent构建: https://idealab.alibaba-inc.com/#/ideaTALK/market/article?articleId=75&marketType=learnCenter
● Agent: https://www.anthropic.com/engineering/building-effective-agents
● tool: https://www.anthropic.com/engineering/writing-tools-for-agents
● 上下文工程: https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents
● harness: https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents
● Agent: https://lilianweng.github.io/posts/2023-06-23-agent/
● ReAct: https://arxiv.org/abs/2210.03629
● CoT: https://arxiv.org/abs/2201.11903
● 反思: https://arxiv.org/abs/2303.11366